class Soldier is LandUnit -> {  
    constructor(number id) -> { 
        number self.id = id; 
    }; 
}; 
& 

function number W(number a) -> { 
    while True -> {
        number b = a + 1;
    }; 
    return a;
};
